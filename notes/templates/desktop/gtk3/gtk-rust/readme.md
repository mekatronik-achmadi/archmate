# Rust FLTK

Install FLTK crate

```sh
cargo add gtk
```

Clean and Build binary

```sh
cargo clean

cargo build --release
```

Run binary

```
cargo run --release
```

or Run without Cargo

```sh
./target/release/gtk_template
```

